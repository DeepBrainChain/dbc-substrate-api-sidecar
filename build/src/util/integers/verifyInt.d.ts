/**
 * Verify all integers including zeroes.
 * @param num
 */
export declare const verifyUInt: (num: number) => boolean;
/**
 * Verify all integers except for zero. Will return false when zero is inputted.
 * @param num
 */
export declare const verifyNonZeroUInt: (num: number) => boolean;
